package com.example.savetheparachutists;

//The data which will be send to the display layer when a scheduled action need to be done
public class ScheduledEvent extends Controller_Display_Communication_Event {

    public ScheduledEvent(Object source) {
        super(source);
    }
}
